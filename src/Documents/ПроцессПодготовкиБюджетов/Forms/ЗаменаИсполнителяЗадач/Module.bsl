
#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ВыполнитьЗаменуИсполнителя(Команда)

	Отказ = Ложь;
	Если Не ПроверитьЗаполнение() Тогда
		Отказ = Истина;
	ИначеЕсли ТекущийИсполнитель = НовыйИсполнитель Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("ru = 'Текущий и новый исполнители не отличаются.';
														|en = 'The current assignee and the new assignee are the same.'"), , , ,
			Отказ);
	КонецЕсли;
	Если Отказ Тогда
		Возврат;
	КонецЕсли;
	
	СтруктураИсполнителей = Новый Структура;
	СтруктураИсполнителей.Вставить("ТекущийИсполнитель", ТекущийИсполнитель);
	СтруктураИсполнителей.Вставить("НовыйИсполнитель", НовыйИсполнитель);
	
	Закрыть(СтруктураИсполнителей);

КонецПроцедуры

#КонецОбласти