#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ЭтоНазначение = Параметры.ЭтоНазначение;
	Если ЭтоНазначение Тогда
		Заголовок = НСтр("ru = 'Назначить роли';
						|en = 'Assign roles'");
		Элементы.Применить.Заголовок = НСтр("ru = 'Назначить роли';
											|en = 'Assign roles'");
	Иначе
		Заголовок = НСтр("ru = 'Снять роли';
						|en = 'Clear roles'");
		Элементы.Применить.Заголовок = НСтр("ru = 'Снять роли';
											|en = 'Clear roles'");
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Применить(Команда)

	СтруктураВозврата = Новый Структура();
	СтруктураВозврата.Вставить("Сборщик",   Сборщик);
	СтруктураВозврата.Вставить("Курьер",    Курьер);
	СтруктураВозврата.Вставить("Диспетчер", Диспетчер);
	
	Закрыть(СтруктураВозврата);
	
КонецПроцедуры

#КонецОбласти
