#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныйПрограммныйИнтерфейс

Процедура ЗарегистрироватьДляНастройки(ПланСчетов, НастройкаФормированияПроводок, ОбъектУчета, Знач ОбъектНастройки, Знач Документ) Экспорт
	
	Если НЕ ЗначениеЗаполнено(ПланСчетов) Тогда
		Возврат;
	ИначеЕсли НЕ ЗначениеЗаполнено(НастройкаФормированияПроводок) Тогда
		Возврат;
	ИначеЕсли НЕ ЗначениеЗаполнено(ОбъектУчета) Тогда
		Возврат;
	ИначеЕсли НЕ ЗначениеЗаполнено(ОбъектНастройки) Тогда
		ОбъектНастройки = Неопределено;
	ИначеЕсли НЕ ЗначениеЗаполнено(Документ) Тогда
		Документ = Неопределено;
	КонецЕсли;
	
	НаборЗаписей = СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.ПланСчетов.Установить(ПланСчетов);
	НаборЗаписей.Отбор.НастройкаФормированияПроводок.Установить(НастройкаФормированияПроводок);
	НаборЗаписей.Отбор.ОбъектУчета.Установить(ОбъектУчета);
	НаборЗаписей.Отбор.ОбъектНастройки.Установить(ОбъектНастройки);
	НаборЗаписей.Отбор.Документ.Установить(Документ);
	НаборЗаписей.Прочитать();
	
	Если НаборЗаписей.Количество() = 0 Тогда
		НоваяЗапись = НаборЗаписей.Добавить();
		НоваяЗапись.ПланСчетов = ПланСчетов;
		НоваяЗапись.НастройкаФормированияПроводок = НастройкаФормированияПроводок;
		НоваяЗапись.ОбъектУчета = ОбъектУчета;
		НоваяЗапись.ОбъектНастройки = ОбъектНастройки;
		НоваяЗапись.Документ = Документ;
		НаборЗаписей.Записать();
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли