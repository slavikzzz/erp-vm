#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ, Замещение)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Для Каждого ЗаписьНабора Из ЭтотОбъект Цикл
		Если НЕ ПустаяСтрока(ЗаписьНабора.ВыражениеЗаполненияСубконто1) Тогда
			ЗаписьНабора.ВыражениеЗаполненияСубконто1 = СтрЗаменить(ЗаписьНабора.ВыражениеЗаполненияСубконто1, "_", ".");
		КонецЕсли;
		Если НЕ ПустаяСтрока(ЗаписьНабора.ВыражениеЗаполненияСубконто2) Тогда
			ЗаписьНабора.ВыражениеЗаполненияСубконто2 = СтрЗаменить(ЗаписьНабора.ВыражениеЗаполненияСубконто2, "_", ".");
		КонецЕсли;
		Если НЕ ПустаяСтрока(ЗаписьНабора.ВыражениеЗаполненияСубконто3) Тогда
			ЗаписьНабора.ВыражениеЗаполненияСубконто3 = СтрЗаменить(ЗаписьНабора.ВыражениеЗаполненияСубконто3, "_", ".");
		КонецЕсли;
		Если НЕ ЗначениеЗаполнено(ЗаписьНабора.УказанноеЗначениеСубконто1) Тогда
			ЗаписьНабора.УказанноеЗначениеСубконто1 = Неопределено;
		КонецЕсли;
		Если НЕ ЗначениеЗаполнено(ЗаписьНабора.УказанноеЗначениеСубконто2) Тогда
			ЗаписьНабора.УказанноеЗначениеСубконто2 = Неопределено;
		КонецЕсли;
		Если НЕ ЗначениеЗаполнено(ЗаписьНабора.УказанноеЗначениеСубконто3) Тогда
			ЗаписьНабора.УказанноеЗначениеСубконто3 = Неопределено;
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли