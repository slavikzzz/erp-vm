
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныеПроцедурыИФункции

// Включает использование шаблона проводки (соответствия счетов / оборотов) в настройку формирования проводок.
// 
// Параметры:
// 	НастройкаФормированияПроводок - СправочникСсылка.НастройкиФормированияПроводокМеждународногоУчета - Настройка, в которую необходимо включить шаблон проводки 
// 	ШаблонПроводки - СправочникСсыока.ШаблоныПроводокДляМеждународногоУчета,
// 					 СправочникСсылка.СоотвествияСчетовМеждународногоУчета
// 					 СправочникСсылка.СоотвествияОборотовМеждународногоУчета - Шаблон проводки (соответствия счетов / оборотов).
//
Процедура ВключитьВНастройкуФормированияПроводок(НастройкаФормированияПроводок, ШаблонПроводки) Экспорт
	
	НаборЗаписей = РегистрыСведений.ПравилаОтраженияВМеждународномУчете.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.НастройкаФормированияПроводок.Установить(НастройкаФормированияПроводок);
	НаборЗаписей.Отбор.ШаблонПроводки.Установить(ШаблонПроводки);
	
	НоваяЗапись = НаборЗаписей.Добавить();
	НоваяЗапись.НастройкаФормированияПроводок = НастройкаФормированияПроводок;
	НоваяЗапись.ШаблонПроводки = ШаблонПроводки;
	
	НаборЗаписей.Записать();
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли
