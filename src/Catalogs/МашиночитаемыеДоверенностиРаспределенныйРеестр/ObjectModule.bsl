#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;

	Если ПометкаУдаления ИЛИ Ссылка.ПометкаУдаления Тогда
		Возврат;
	КонецЕсли;
	
	Наименование = ?(РежимыИспользования = "ФТС", НСтр("ru = 'Машиночитаемая доверенность (ФТС)';
														|en = 'Машиночитаемая доверенность (ФТС)'"),
		НСтр("ru = 'Машиночитаемая доверенность (ФНС, распределенный реестр)';
			|en = 'Машиночитаемая доверенность (ФНС, распределенный реестр)'"))
		+ ?(ЗначениеЗаполнено(НомерДоверенности), " " + НСтр("ru = '№';
															|en = '№'") + НомерДоверенности, "")
		+ ?(ЗначениеЗаполнено(ДатаВыдачи), " " + НСтр("ru = 'от';
														|en = 'от'") + " "
		+ Формат(ДатаВыдачи, "ДФ='дд.ММ.гггг'") + " " + НСтр("ru = 'г.';
															|en = 'г.'"), "");
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли